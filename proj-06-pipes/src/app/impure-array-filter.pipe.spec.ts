import { ImpureArrayFilterPipe } from './impure-array-filter.pipe';

describe('ImpureArrayFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ImpureArrayFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
